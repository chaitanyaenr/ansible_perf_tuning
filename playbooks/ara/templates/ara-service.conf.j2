[unit]
Description=ara
After=network.target

[Service]
Type=simple
TimeoutStartSec=0
Restart=on-failure
RestartSec=10
RemainAfterExit=yes
ExecStart=/usr/bin/ara-manage runserver -h {{ host }} -p {{ port }}

[Install]
WantedBy=multi-user.target
